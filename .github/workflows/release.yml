name: Create Release

on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  build-and-release:
    name: Build & Release Android APK
    runs-on: ubuntu-22.04  # Pinned to x86_64 — hermesc binary is not ARM64 compatible
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Make hermesc executable
        run: |
          HERMESC="node_modules/react-native/sdks/hermesc/linux64-bin/hermesc"
          if [ -f "$HERMESC" ]; then
            chmod +x "$HERMESC"
            echo "hermesc found and made executable"
          else
            echo "hermesc not found at $HERMESC — listing available:"
            ls node_modules/react-native/sdks/hermesc/ || true
          fi

      - name: Cache Gradle packages
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-home-cache-cleanup: true

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build release APK (arm64-v8a)
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon --stacktrace
        env:
          NODE_OPTIONS: --max-old-space-size=4096

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: android/app/build/outputs/apk/release/app-release.apk
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
